import SwiftUI

struct ListView: View {
    var body: some View {
        Text("Hello, world!")
            .padding()
    }
}

struct ListView_Previews: PreviewProvider {
    static var previews: some View {
        ListView()
    }
}
